# Advanced Linux Server Monitoring & Management Bot

🚀 **Enterprise-ready** система мониторинга и управления инфраструктурой через Telegram с элементами ChatOps и Self-Healing.

## 🎯 Проект
Демонстрация навыков DevOps Engineer: разработка асинхронного Python-бота, статистический анализ данных, визуализация, контейнеризация и реализация ChatOps-воркфлоу для управления Docker-контейнерами.

## 🏗 Архитектура
Проект объединяет в себе модуль мониторинга, систему анализа аномалий и инструменты управления сервером без необходимости открывать внешние порты (Security by Design).

```
server-monitoring-bot/
├── bot/                  # Ядро приложения
│   ├── main.py           # Точка входа и настройка очередей задач
│   ├── handlers.py       # Логика команд (вкл. Docker & Graphs)
│   ├── metrics.py        # Сбор метрик (psutil) и статистический анализ
│   ├── alerts.py         # Умная система алертов (2-Sigma)
│   ├── graphs.py         # Генерация визуализации (Matplotlib)
│   └── config.py         # Управление конфигурацией
├── docker/               # Контейнеризация
├── .github/              # CI/CD пайплайны
└── secrets/              # Хранение токенов (не в репозитории)
```

## 🛠 Технологический стек
* **Язык:** Python 3.10+ (Asyncio)
* **Telegram API:** `python-telegram-bot` (v20)
* **Метрики & Аналитика:** `psutil`, `statistics`
* **Визуализация:** `matplotlib`
* **Инфраструктура:** Docker, Docker Compose
* **CI/CD:** GitHub Actions (Lint, Build, Security Scan)

## ✨ Возможности (Features)

### 📊 Observability & Monitoring
*   **Real-time Metrics:** Сбор данных CPU, RAM, Disk, Uptime, Load Average.
*   **Smart Alerts:** Адаптивная система алертов на основе **правила 2-х сигм (Standard Deviation)**. Умное обнаружение аномалий вместо жестких порогов.
*   **Data Visualization:** Генерация графиков использования памяти (Pie Charts) прямо в оперативной памяти с отправкой в Telegram (без сохранения на диск).

### 🤖 ChatOps & Management
*   **Docker Control:** Полный контроль над контейнерами прямо из чата:
    *   `/ps` — Список запущенных контейнеров.
    *   `/logs <name>` — Чтение логов в реальном времени.
    *   `/restart <name>` — Перезагрузка сервисов.
*   **Self-Healing:** Автоматическое реагирование на критические состояния (например, очистка Docker cache при переполнении диска).

### 🔒 Security & Ops
*   **Zero Trust:** Проверка `TELEGRAM_USER_ID` для всех команд.
*   **No exposed ports:** Бот работает через Long Polling, нет открытых HTTP/Websocket портов.
*   **Secrets Management:** Поддержка Docker Secrets и `.env` файлов.

## 🚀 Установка и запуск

### 1. Настройка окружения
Скопируй файл переменных:
```bash
cp .env.example .env

Отредактируй `.env`:
```ini
BOT_TOKEN=твой_токен_от_botfather
TELEGRAM_USER_ID=твой_id
```

### 2. Запуск через Docker (Рекомендуемый)

Для работы функций управления Docker (ChatOps) необходимо пробросить сокет:

```yaml
# docker/docker-compose.yml
version: '3.8'
services:
  bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: always
    env_file:
      - ../.env
    volumes:
      # Проброс сокета для управления docker изнутри контейнера
      - /var/run/docker.sock:/var/run/docker.sock
    # Примечание: Для команд docker контейнер может потребовать прав root
```

Запуск:
```bash
cd docker
docker-compose up -d --build
```

## 📝 Команды бота

| Команда | Описание |
| :--- | :--- |
| `/start` | Инициализация и проверка доступа |
| `/help` | Справка |
| `/status` | Полная сводка метрик сервера |
| `/cpu` | Загрузка процессора |
| `/ram` | Использование памяти |
| `/disk` | Место на диске |
| `/uptime` | Время работы |
| `/graph` | 📈 **Визуализация RAM (Pie Chart)** |
| `/alerts` | Статус активных аномалий |
| `/ps` | 🐳 Список Docker контейнеров |
| `/logs <name>` | 📋 Логи контейнера |
| `/fix` | 🩹 Запуск процедуры очистки диска (Self-Healing) |

## 🧠 Как это работает (Deep Dive)

### 1. Адаптивные алерты
Вместо `if cpu > 90` мы используем скользящее окно и среднеквадратичное отклонение.
```python
threshold = mean + (2 * stdev)
```
Это позволяет игнорировать обычные пиковые нагрузки и реагировать только на реальные аномалии.

### 2. Визуализация без файлов
Используется библиотека `matplotlib` с backend `Agg` (Non-Interactive). Графики рендерятся в объект `io.BytesIO` в RAM и отправляются как `InputFile`, не засоряя диск сервера временными файлами.

### 3. ChatOps
Использование `subprocess` для взаимодействия с Docker Daemon через UNIX socket (`/var/run/docker.sock`), что позволяет реализовать полноценный CLI внутри мессенджера.
