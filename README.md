Linux Server Monitoring Bot
Простой мониторинг Linux-сервера с оповещениями в Telegram.

🎯 Цель
Демонстрация навыков DevOps: разработка Python-бота, работа с системными метриками, контейнеризация (Docker) и настройка CI/CD (GitHub Actions).

🏗 Архитектура
Проект состоит из Telegram-бота, который собирает метрики хост-системы (через psutil) и отправляет их пользователю или генерирует алерты.

server-monitoring-bot/
├── bot/ # Исходный код
├── docker/ # Файлы для контейнеризации
└── .github/ # CI/CD пайплайны


## 🛠 Стек
* **Язык:** Python 3.13+
* **Библиотеки:** `python-telegram-bot`, `psutil`
* **Инфраструктура:** Docker, Docker Compose
* **CI/CD:** GitHub Actions (Lint, Build)

## ⚙️ Переменные окружения
Создайте файл `.env` в корне проекта на основе `.env.example`:
* `BOT_TOKEN` — токен от @BotFather
* `TELEGRAM_USER_ID` — ваш ID (узнать у @userinfobot)
* `CHECK_INTERVAL` — частота проверки метрик (сек)
* `ALERT_COOLDOWN` — задержка между спам-алертами (сек)

## 📝 Команды бота
/start — Проверка доступа
/status — Общая сводка (CPU, RAM, Disk, Uptime)
/cpu — Загрузка процессора
/ram — Использование памяти
/disk — Место на диске
/uptime — Время работы
/alerts — Статус активных алертов

## 🚀 Как запустить

### Вариант 1: Локально
1. Клонировать репозиторий
2. Создать `.env` файл
3. Установить зависимости: `pip install -r requirements.txt`
4. Запустить: `python -m bot.main`

### Вариант 2: Docker (Рекомендуемый)
Находясь в папке `docker`, выполните:

docker-compose up -d --build

🚨 Система алертов
Бот автоматически отправляет уведомления в Telegram, если:

CPU > 85%
RAM > 90%
Disk > 90%
Имеется защита от флуда (cooldown).
